<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartchat.dao.ChatDao">

    <select id="selectAll" resultType="com.smartchat.dto.ChatDto">
        SELECT QA_ID AS qaId,
               QUESTION AS question,
               ANSWER AS answer
        FROM CHATBOT_QA
        ORDER BY QA_ID ASC
    </select>

    <select id="selectById" parameterType="int" resultType="com.smartchat.dto.ChatDto">
        SELECT QA_ID AS qaId,
               QUESTION AS question,
               ANSWER AS answer
        FROM CHATBOT_QA
        WHERE QA_ID = #{qaId}
    </select>

    <insert id="insertQA" parameterType="com.smartchat.dto.ChatDto">
        INSERT INTO CHATBOT_QA (QA_ID, QUESTION, ANSWER)
        VALUES (CHATBOT_QA_SEQ.NEXTVAL, #{question}, #{answer})
    </insert>

    <update id="updateQA" parameterType="com.smartchat.dto.ChatDto">
        UPDATE CHATBOT_QA
        SET QUESTION = #{question},
            ANSWER = #{answer}
        WHERE QA_ID = #{qaId}
    </update>

    <delete id="deleteQA" parameterType="int">
        DELETE FROM CHATBOT_QA WHERE QA_ID = #{qaId}
    </delete>

</mapper>
